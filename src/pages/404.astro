---
import Base from '../layouts/Base.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
---

<Base title="404 — Page Not Found">
  <Nav />
  <main class="not-found">
    <div class="not-found-inner">
      <p class="nf-code">404</p>
      <h1 class="nf-title">Lost in the void.</h1>
      <p class="nf-sub">This page doesn't exist, or it moved somewhere else.</p>
      <a href="/" class="nf-link">← Back to home</a>
    </div>
  </main>
  <Footer />
</Base>

<style>
  .not-found {
    min-height: calc(100vh - var(--nav-height) - 200px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 4rem 2rem;
    text-align: center;
  }

/*  .not-found-inner {
    max-width: 480px;
  }

  .nf-code {
    font-family: var(--font-display);
    font-size: 8rem;
    line-height: 1;
    letter-spacing: -0.05em;
    color: var(--border);
    margin-bottom: 1rem;
  }

  .nf-title {
    font-family: var(--font-display);
    font-size: 2.5rem;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
  }

  .nf-sub {
    font-size: 16px;
    color: var(--muted);
    margin-bottom: 2.5rem;
    line-height: 1.6;
  }

  .nf-link {
    font-size: 15px;
    font-family: var(--font-mono);
    color: var(--accent);
    letter-spacing: 0.03em;
    transition: color 0.15s;
  }

  .nf-link:hover { color: var(--ink); }*/
</style>
